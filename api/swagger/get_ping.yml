Check the health (connectivity) of the API
Route to check the API connectivity
---
definitions:
  - schema:
      id: Yello
      properties:
        message:
         type: string
         description: Yello World !
responses:
  200:
    description: >
      Simple message `Yello World !`
    schema:
      type: object
      properties:
        message:
          type: string
